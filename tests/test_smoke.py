"""Basic smoke test so pytest always collects at least one test in CI.

If you later add real tests, keep this or replace it with a meaningful
import-level smoke test."""

def test_smoke():
    assert True
